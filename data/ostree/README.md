# OSTree Remote and Update Configuration

This directory will contain OSTree remote definitions and update
configuration files. Populated in Sub-Phase 4.5 (Atomic Updates).

## Planned Contents

- `zenos.conf` — OSTree remote configuration (URL, GPG key)
- `update-policy.conf` — Update check interval, auto-download settings
